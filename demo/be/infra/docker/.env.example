# Local MySQL credentials for Docker Compose (copy to .env — never commit .env)
MYSQL_ROOT_PASSWORD=your_local_root_password
MYSQL_DATABASE=memory_rag
MYSQL_USER=memory_rag
MYSQL_PASSWORD=your_local_db_password
